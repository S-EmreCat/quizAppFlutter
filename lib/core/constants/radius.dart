import 'package:flutter/widgets.dart';

class RadiusPage extends BorderRadius {
  const RadiusPage.all20() : super.all(const Radius.circular(20));

  // burada türetip sayfada direkt nasıl kullanacağımı bulamadım hata veriyor (Type)

}

class RadiusConstants {
  static double yirmi = 20.0;
}
